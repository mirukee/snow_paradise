import 'package:flutter/foundation.dart';
import 'package:image_picker/image_picker.dart';

class HeicConverter {
  static Future<Uint8List?> convertToJpeg(XFile file) async {
    // Mobile handles HEIC natively or via flutter_image_compress
    return null; 
  }
}
